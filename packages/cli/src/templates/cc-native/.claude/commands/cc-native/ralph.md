---
description: Run a Ralph Wiggum autonomous improvement loop
---
# Ralph Loop

Launch an autonomous coding loop where each iteration gets a fresh Claude Code context window. Ralph picks tasks, implements them, verifies quality gates, and commits — one task per iteration.

Load and execute `_cc-native/workflows/ralph.md`.

Follow the Intake → Pre-flight → Decompose → Craft → Initialize → Generate → Execute → Report pattern.
