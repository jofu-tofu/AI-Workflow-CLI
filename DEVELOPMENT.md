# Development Guide - AI Workflow CLI

## Before Starting Development

Read this section before any development or testing work. These steps prevent path errors, test failures, and cross-environment pollution.

### Step 1: Set AIW_DIR Environment Variable

Navigate to your development worktree root and set:

**PowerShell (Windows):**
```powershell
$env:AIW_DIR = $PWD.Path
```

**Bash (Unix/Git Bash):**
```bash
export AIW_DIR="$(pwd)"
```

### Step 2: Verify Configuration

```bash
# PowerShell
echo $env:AIW_DIR

# Bash
echo $AIW_DIR
```

Output should show your current worktree path.

### Step 3: Run Tests

Tests run from `packages/cli/` using npm (not bun):

```bash
cd packages/cli
npm test
```

**Success:** All tests pass
**Failure:** If tests fail with path errors, AIW_DIR is not set correctly—repeat Step 1

---

## Architecture Understanding

### Working Directory vs Template Directory

| Location | Purpose | When to Modify |
|----------|---------|----------------|
| `.aiwcli/` | Runtime hooks and libraries | During development |
| `packages/cli/src/templates/` | Distribution templates | After `.aiwcli/` changes |

**Synchronization Rule:** Changes to `.aiwcli/` must be synchronized to `packages/cli/src/templates/`. This ensures new project initializations receive updates.

### Template Synchronization

**When modifying hooks, library code, or settings in `.aiwcli/`:**

Runtime files execute from `.aiwcli/_core/` in this repo. Template source lives under `packages/cli/src/templates/core/` and is normalized to `_core` during install.

**Files that need synchronization:**
- `.aiwcli/_core/hooks-ts/*.ts` → `packages/cli/src/templates/core/hooks-ts/`
- `.aiwcli/_core/lib-ts/**/*.ts` → `packages/cli/src/templates/core/lib-ts/`
- `.aiwcli/_cc-native/**` → `packages/cli/src/templates/cc-native/_cc-native/`
- `.aiwcli/_core/skills/handoff-system/**` → `packages/cli/src/templates/core/skills/handoff-system/`
- `.aiwcli/_core/skills/meta-plan/**` → `packages/cli/src/templates/core/skills/meta-plan/`
- `.aiwcli/_core/skills/codex/**` → `packages/cli/src/templates/core/skills/codex/`
- `.aiwcli/_core/skills/devin/**` → `packages/cli/src/templates/core/skills/devin/`
- `.aiwcli/_core/scripts/*.ts` → `packages/cli/src/templates/core/scripts/`
- `.aiwcli/_core/lib-ts/context/**` → `packages/cli/src/templates/core/lib-ts/context/`
- `.aiwcli/_core/lib-ts/runtime/**` → `packages/cli/src/templates/core/lib-ts/runtime/`
- `.aiwcli/_core/lib-ts/hooks/**` → `packages/cli/src/templates/core/lib-ts/hooks/`
- `.aiwcli/_core/lib-ts/templates/**` → `packages/cli/src/templates/core/lib-ts/templates/`
- `.aiwcli/_core/lib-ts/agent-exec/**` → `packages/cli/src/templates/core/lib-ts/agent-exec/`
- `.aiwcli/_core/lib-ts/types.ts` → `packages/cli/src/templates/core/lib-ts/types.ts`
- `.aiwcli/_core/lib-ts/tsconfig.json` → `packages/cli/src/templates/core/lib-ts/tsconfig.json`
- `.aiwcli/_core/lib-ts/package.json` → `packages/cli/src/templates/core/lib-ts/package.json`
- `.aiwcli/_core/skills/*/SKILL.md` → `packages/cli/src/templates/core/skills/*/SKILL.md`
- `.codex/workflows/` → `packages/cli/src/templates/core/.codex/workflows/`
- `.cognition/` → `packages/cli/src/templates/core/.cognition/`
- `.claude/settings.json` → generated by merge/reconstruction (`src/lib/core-ide-base.ts` + `src/lib/template-settings-reconstructor.ts`)

**When to sync:**
- Adding new hooks
- Modifying hook behavior
- Adding/changing library functions used by hooks
- Updating settings.json hook configurations
- Moving runtime files inside `.aiwcli/_core/`
- Updating `.aiwcli/_cc-native/` runtime files that are mirrored into the cc-native template

**Sync commands:**

```bash
# Sync cc-native runtime tree into its template mirror
cd packages/cli
npm run sync:cc-native

# Sync extracted shared library modules into CLI and template wrappers
npm run sync:shared-lib
```

**Note:** The `dist/` directory is auto-generated during build — only update `src/templates/`.

### Directory Structure

```
.aiwcli/
├── _core/                      # Cross-method infrastructure
│   ├── hooks-ts/               # Shared TypeScript hook scripts (run via bun)
│   │   ├── user_prompt_submit.ts    # Context binding
│   │   ├── context_monitor.ts       # Context usage monitoring
│   │   ├── archive_plan.ts          # Plan archival on ExitPlanMode
│   │   ├── session_start.ts         # Context restoration on session start
│   │   ├── session_end.ts           # State save and mode staging
│   │   ├── pre_compact.ts           # Pre-compaction state snapshot
│   │   ├── file-suggestion.ts       # File organization suggestions
│   │   ├── task_create_capture.ts   # Task persistence
│   │   ├── task_update_capture.ts   # Task status changes
│   │   ├── codex_explorer.ts        # Codex explorer agent integration
│   │   └── lint_after_edit.ts       # Post-edit lint dispatching
│   ├── lib-ts/
│   │   ├── hooks/              # Hook lifecycle
│   │   │   ├── hook-utils.ts        # Hook lifecycle, stdin parsing, output emit
│   │   │   ├── context-monitor-logic.ts  # Context monitor extracted logic
│   │   │   ├── prompt-binding-logic.ts   # Prompt binding extracted logic
│   │   │   └── session-end-logic.ts      # Session end extracted logic
│   │   ├── runtime/            # Core utilities
│   │   │   ├── atomic-write.ts      # Cross-platform crash-safe writes
│   │   │   ├── constants.ts         # Security constants, paths
│   │   │   ├── logger.ts            # Unified JSONL logger
│   │   │   ├── inference.ts         # Inference model utilities
│   │   │   ├── subprocess-utils.ts  # Subprocess and internal call detection
│   │   │   ├── utils.ts             # Common functions
│   │   │   ├── state-io.ts          # State serialization with migration
│   │   │   ├── git-state.ts         # Git snapshot
│   │   │   ├── lint-dispatch.ts     # Post-edit lint dispatching
│   │   │   ├── aiw-cli.ts           # AIW CLI integration
│   │   │   ├── cli-args.ts          # CLI arg generation
│   │   │   └── stop-words.ts        # Word list for ID generation
│   │   ├── context/            # Context management
│   │   │   ├── context-store.ts     # CRUD operations, state persistence
│   │   │   ├── context-selector.ts  # Context determination and matching
│   │   │   ├── context-formatter.ts # Display formatting
│   │   │   ├── plan-manager.ts      # Plan archival, hash, sign
│   │   │   └── task-tracker.ts      # Task CRUD on state.json
│   │   ├── agent-exec/         # Agent execution infrastructure
│   │   ├── templates/          # Output formatters
│   │   │   ├── formatters.ts        # Mode maps, icons
│   │   │   └── plan-context.ts      # Plan evaluation templates
│   │   └── types.ts            # Shared types
│   └── skills/
│       ├── handoff-system/     # Session handoff creation and restoration
│       ├── codex/              # Codex pane launcher skill
│       └── meta-plan/          # Prompt amplification for complex problems
│
└── _cc-native/                 # Claude Code method-specific code
    ├── hooks/
    │   ├── cc-native-plan-review.ts         # Multi-step plan review (async)
    │   ├── mark_questions_asked.ts          # Marks questions-asked state
    │   ├── enhance_plan_post_subagent.ts    # Post-subagent plan enhancement
    │   ├── enhance_plan_post_write.ts       # Post-write plan enhancement
    │   ├── plan_questions_early.ts          # Phase A clarification prompt
    │   └── validate_task_prompt.ts          # Task creation prompt validation
    ├── lib-ts/                 # CC-native shared library
    ├── artifacts/              # Review artifact generation
    ├── plan-review/            # Multi-agent plan review pipeline
    └── agents/                 # Agent spec files

_output/
├── index.json                   # Global context cache
├── hook-log.jsonl               # Diagnostic logs (JSONL format)
└── contexts/                    # Context state management
    └── {context-id}/
        ├── state.json           # SOURCE OF TRUTH per context
        ├── plans/               # Archived approved plans
        ├── handoffs/            # Handoff documents
        ├── reviews/             # Plan review artifacts
        └── session-transcripts/ # JSONL session records
```

### State Model

Each context has a `state.json` file as its source of truth. A global `_output/index.json` caches context metadata for fast lookup.

**Context modes:**

| Mode | Meaning |
|------|---------|
| `created` | Context created, no session bound yet |
| `active` | Session actively working in this context |
| `has_staged_work` | Session ended with unsaved artifact (plan or handoff) |
| `archived` | Context completed or abandoned |

**Mode transitions:**
- `created` → `active` (session bound via `maybeActivate`)
- `active` → `has_staged_work` (session end with unconsumed artifact)
- `has_staged_work` → `active` (session start restores context)

### Hook System

Hooks are TypeScript scripts run via Bun, triggered by Claude Code lifecycle events. Configuration lives in `.claude/settings.json`.

**Shared Hooks** (`.aiwcli/_core/hooks-ts/` — 11 hooks):
- `user_prompt_submit.ts` — Context enforcement, session binding
- `context_monitor.ts` — Context usage monitoring (30%/20%/10% warnings)
- `session_start.ts` — Context restoration on session start
- `session_end.ts` — State save and mode staging
- `archive_plan.ts` — Archives approved plans on ExitPlanMode
- `pre_compact.ts` — Pre-compaction state snapshot
- `file-suggestion.ts` — File organization suggestions
- `task_create_capture.ts` — Captures task creation events
- `task_update_capture.ts` — Captures task status changes
- `codex_explorer.ts` — Codex explorer agent integration
- `lint_after_edit.ts` — Post-edit lint dispatching

**CC-Native Hooks** (`.aiwcli/_cc-native/hooks/` — 6 hooks):
- `cc-native-plan-review.ts` — Multi-step plan review (CLI + agents)
- `mark_questions_asked.ts` — Marks questions-asked state after user answers
- `enhance_plan_post_subagent.ts` — Post-subagent plan enhancement
- `enhance_plan_post_write.ts` — Post-write plan enhancement
- `plan_questions_early.ts` — Phase A clarification prompt in plan mode
- `validate_task_prompt.ts` — Task creation prompt validation

---

## Development Workflow

### Modifying Hooks

1. Edit the hook in `.aiwcli/_core/hooks-ts/` or `.aiwcli/_cc-native/hooks/`
2. Test by running Claude Code with the modified hook
3. Synchronize to `packages/cli/src/templates/`
4. Run tests: `cd packages/cli && npm test`

### Modifying Libraries

1. Edit the library in `.aiwcli/_core/lib-ts/` or `.aiwcli/_cc-native/lib-ts/`
2. Test dependent hooks manually
3. Synchronize to `packages/cli/src/templates/`
4. Run tests: `cd packages/cli && npm test`

### Adding New Hooks

1. Create the hook script in the appropriate directory
2. Use `runHook()` or `runHookAsync()` as entry point (from `hooks/hook-utils.ts`)
3. Add hook wiring to `.claude/settings.json`
4. Synchronize both the hook and settings.json to the template directory

---

## Running Tests

All test commands run from `packages/cli/`:

```bash
cd packages/cli

npm test              # All tests
npm run test:unit     # Unit tests only
npm run test:integration  # Integration tests only
npm run test:watch    # Watch mode
npm run test:coverage # With coverage
```

**Important:** Use `npm test`, not `bun test`. Tests use Mocha, not Bun's test framework.

---

## Watch Mode Development

```bash
cd packages/cli

npm run watch         # Rebuilds code AND runs tests on changes
npm run dev:watch     # TypeScript compilation + template sync
npm run test:watch    # Mocha in watch mode
```

---

## Core Package Source of Truth

Reusable cross-platform utility code lives in `packages/cli/src/lib/runtime/`.
Current extracted modules: `tmux-preflight.ts`, `executable-policy.ts`, `platform-adapter.ts`, `subprocess-utils.ts`, `sentinel-ipc.ts`.

---

## Troubleshooting

### Tests fail with "path not found" errors

**Cause:** AIW_DIR environment variable not set
**Fix:** Set AIW_DIR to worktree root (see Step 1 above)

### Files created in unexpected locations

**Cause:** AIW_DIR points to wrong directory
**Fix:** Verify AIW_DIR matches current directory

### Hook changes not taking effect

**Cause:** Template directory not synchronized
**Fix:** Run sync commands (see Template Synchronization above)

---

## Environment Configuration

| Environment | AIW_DIR Value | Purpose |
|-------------|---------------|---------|
| Development | `$(pwd)` (worktree root) | Isolated testing in development branch |
| Production | `~/.aiw` or `$HOME\.aiw` | Deployed global AI Workflow CLI |
